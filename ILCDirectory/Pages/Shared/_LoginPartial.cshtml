<ul class="navbar-nav">
@*    @if (User.Identity.IsAuthenticated)
    {
        <li><a asp-controller="Account" asp-action="ChangePassword">Hello @User.Identity.Name</a></li>
        <li><a asp-controller="Account" asp-action="Logout">Logout</a></li>
    }
    else
    {
        <li><a asp-controller="Account" asp-action="Login">Login</a></li>
    }
*@
    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <a  class="nav-link text-dark" href="/Account/Index" title="Manage">Hello @User.Identity?.Name!</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" href="/Account/Logout">
                <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Account" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
